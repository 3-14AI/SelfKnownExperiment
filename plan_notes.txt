Thinking:
The "Next Steps" section is empty.
The task says "Read agents.md and perform the next step of project development".
If "Next Steps" is empty, memory says:
"When the `## Next Steps` section in `agents.md` is empty (or only contains completed items like `- [x]`), check the `## Completed` section for recently implemented traits lacking unit tests. If all recent features are adequately tested, propose and implement ONE meaningful new feature or trait to maintain momentum."

I checked:
- is_gluttonous: tested
- is_filter_feeder: tested
- is_mud_bather: tested
- has_blubber: tested
- can_sweat: tested
- is_detritivore: tested
- is_vampiric: tested
- can_sprint: tested
- pack_hunter: tested
- is_scentless: tested
- is_nocturnal_predator: tested
- disease_vector: tested
- is_carnivorous_plant: tested
- is_desertic: tested
- has_bioluminescence: tested
- can_climb: tested
- has_scales: tested
- has_claws: tested
- is_electric: tested
- is_cold_blooded: tested
- is_fruiting: tested
- is_aposematic: tested
- has_echolocation: tested
- has_shell: tested
- can_photosynthesize: tested
- is_venomous: tested
- can_spin_webs: tested
- has_spikes: tested
- can_hibernate: tested
- is_immune: tested
- is_regenerative: tested
- is_parasitic: tested
- has_fur: tested
- is_volcanic: tested
- is_forestal: tested
- is_social: tested

All recent features have unit tests (mutations and mostly mechanics).
I need to propose and implement ONE meaningful new feature or trait.

Let's implement `has_horns` trait.
Description: Entities with `has_horns` get a +2 attack bonus. If they are herbivores and attacked, they also get a +2 defense bonus, and the attacker takes 1 damage when attacking. Let's make it simpler: +2 attack for all, and +2 defense for herbivores/omnivores against predators, or just a flat +3 attack when hunting, and +3 defense when being hunted.

Actually, let's implement `can_leap`.
Trait: `can_leap`. Entities with this trait can move 2 tiles at once instead of 1, costing slightly more stamina.

Or let's implement `is_cannibal`.
Trait: `is_cannibal`. Entities with this trait can attack and eat entities of the same species.

Or `has_horns`: Entities with `has_horns` deal flat damage back to attackers (like spikes, but different? We have has_spikes).

Let's do `has_horns`: Entities with this trait gain a flat +3 attack bonus in combat, and can break through 'wall' terrain slowly (destroying the wall, replacing it with grass).
Wait, engine doesn't support destroying walls easily without extra logic.

How about `is_toxic` / `is_poisonous`? We have `is_venomous` (poisons opponent on attack), `toxicity` (poisons predator when eaten).

How about `is_scavenger` trait? No, diet is 'scavenger'.

How about `is_herd_animal`? `is_social` already exists.

How about `has_medicinal`? Already exists for plants (`plant_type == 'medicinal'`).

How about `can_camouflage`? We have `camouflage`.

How about `can_dig` / `is_digger`? We have `can_burrow`.

How about `is_territorial`? Entities with `is_territorial` gain +5 attack and +5 defense when they are near their "home" (spawn location).

How about `is_migratory`?

How about `has_chitin`? Like `has_shell` or `has_scales`.

How about `has_gills`? We have `is_aquatic`.

How about `can_jump`? Let's implement `can_jump` trait. Entities with this trait can bypass 1-tile gaps of impassable terrain (like water or wall).

How about `has_tusks`? Entities with `has_tusks` gain +3 attack and can consume 'wood' terrain for energy?

How about `is_eusocial`?

Let's implement `has_stinger`. Entities with this trait have a high chance to poison their opponent in combat, but take 5 damage when they use it.

Let's do `is_territorial`. When an entity is created, it stores `home_x` and `home_y`. If it fights within 5 tiles of its home, it gets +5 attack and +5 defense.

Or let's implement `can_charge`. Entities with `can_charge` gain a massive attack bonus (+10) but lose all their stamina when initiating an attack.

Or `is_ambush_predator`. Entities with this trait gain +10 attack if they haven't moved in the last 3 ticks.
Let's add `is_ambush_predator`!
- Add `is_ambush_predator` to Entity `__init__`.
- Add `ticks_since_move` to Entity, initialized to 0. Increment it each tick if `can_move` is True and it didn't move. Reset to 0 when it moves.
- In `resolve_combat` (or where attack happens), if `is_ambush_predator` is True and `ticks_since_move >= 3`, gain +10 `effective_attack`.

Wait, in tick:
```python
                    if can_move:
                        moved_for_water = False
```
Finding where movement actually happens.
```python
                    if dx != 0 or dy != 0:
                        try:
                            self.move_entity(entity, dx, dy)
                            entity.ticks_since_move = 0
                        except ValueError:
                            entity.ticks_since_move += 1
                    else:
                        entity.ticks_since_move += 1
```

Let's do something simpler: `is_symbiotic`.
Actually, we already have `symbiotic_with`.

Let's implement `has_tentacles`.
Entities with `has_tentacles` gain +3 attack and can attack prey from 2 tiles away instead of having to be on the same tile!
Wait, combat logic in engine currently requires: `preys_here = self.get_preys_at(entity.x, entity.y, entity=entity)`.
To support `has_tentacles`, it could search `abs(e.x - entity.x) + abs(e.y - entity.y) <= 1`.

Let's implement `is_scavenger_bird`. (or `is_carrion_feeder`).

Let's look at `is_carnivorous_plant`.
Let's do `has_trunk`. Entities with `has_trunk` can drink water from 2 tiles away.
In `get_nearest_water`: `max_distance=effective_perception`.
If `hydration <= max_hydration / 2` and `has_trunk`: if `nearest_water` distance <= 2, drinks.
Currently, they drink when adjacent to water.
```python
            # Drink water
            water_tiles = [t for t in self.get_terrains_at(entity.x, entity.y) if t.terrain_type in ['water', 'deep-water']]
            if not water_tiles:
                adjacent = self.get_adjacent_positions(entity.x, entity.y)
                for ax, ay in adjacent:
                    if any(t.terrain_type in ['water', 'deep-water'] for t in self.get_terrains_at(ax, ay)):
                        water_tiles = True
                        break
            # + has_trunk logic
            if not water_tiles and getattr(entity, 'has_trunk', False):
                for ax, ay in self.get_positions_in_radius(entity.x, entity.y, 2):
                     if any(t.terrain_type in ['water', 'deep-water'] for t in self.get_terrains_at(ax, ay)):
                         water_tiles = True
                         break
```

Let's do `is_herbivorous_insect` -> no.

Let's implement **`has_chitin`** trait.
- Gives a flat +3 defense.
- Reduces stamina loss from movement by 1 (minimum 0).
- Offspring inherit it.
- Render 'k' if they have it?

Let's implement **`is_venomous`**. Wait, already implemented.

Let's implement **`can_roar`**. Entities with `can_roar` periodically scare away herbivores.
If `can_roar` and `energy > 20` and `time % 10 == 0`:
Find prey within 5 tiles. For each prey, they lose 2 stamina and move 1 tile away from the roarer.

Let's implement **`is_pest`**. Entities with `is_pest` consume 2x food items on a tile when they eat, but gain energy as if they only ate 1. They destroy crops.

Let's implement **`is_frost_resistant`**. We have `is_cold_blooded` and `has_fur`.

Let's implement **`can_swim`**. We have `is_aquatic`, `is_amphibious`.

Let's implement **`has_heavy_hooves`**.
Entities with `has_heavy_hooves` gain +2 attack and ignore the stamina penalty for moving uphill (elevation).
In `engine.py` movement:
```python
        if hasattr(entity, 'stamina'):
            stamina_cost = 1
            if elevation_diff > 0 and not getattr(entity, 'has_heavy_hooves', False):
                stamina_cost += elevation_diff
```

Let's check engine `move_entity` for elevation:
```python
        if hasattr(entity, 'stamina'):
            stamina_cost = 1
            if getattr(entity, 'can_climb', False) and any(t.terrain_type == 'wall' for t in terrains_here):
                stamina_cost += 2

            if not getattr(entity, 'is_flying', False):
                if elevation_diff > 0:
                    stamina_cost += int(elevation_diff)
                elif elevation_diff < -1:
                    # Fall damage logic
                    fall_damage = int((abs(elevation_diff) - 1) * 5)
                    entity.energy = max(0, entity.energy - fall_damage)
```
Let's add `has_heavy_hooves`.
Wait, fall damage is for falling. What about `has_heavy_hooves` avoiding uphill penalty? That's simple and effective.

Let's think of another one: **`is_omnidirectional`** (no, doesn't make sense).
**`is_opportunistic`**: Carnivores with this trait will eat plant food if they are starving (energy < max_energy * 0.2) and no prey is nearby.
Oh, omnivores already exist.

How about **`is_cannibalistic`** trait?
Carnivores with this trait can attack and eat entities of the SAME species if they are hungry (energy < max_energy * 0.5).
Currently in `get_preys_at`:
```python
    def get_preys_at(self, x, y, entity=None):
        preys = [e for e in self.entities if e.x == x and e.y == y and e.diet in ['herbivore', 'scavenger', 'omnivore'] and e.is_alive and e != entity]
        if entity and entity.target_species is not None:
            preys = [p for p in preys if p.species in entity.target_species]
        if entity and getattr(entity, 'is_cannibalistic', False) and entity.energy < entity.max_energy * 0.5:
            # allow eating own species if they are here
            same_species = [e for e in self.entities if e.x == x and e.y == y and e.species == entity.species and e.is_alive and e != entity]
            preys.extend(same_species)
```
Wait, if it's a carnivore, `e.diet in ['herbivore', 'scavenger', 'omnivore']` doesn't include other carnivores. So cannibalism could allow eating ANY entity (including carnivores and same species).
```python
        if entity and getattr(entity, 'is_cannibalistic', False) and entity.energy < entity.max_energy * 0.5:
            same_species = [e for e in self.entities if e.x == x and e.y == y and e.species == entity.species and e.is_alive and e != entity and e not in preys]
            preys.extend(same_species)
```

Let's do **`is_cannibalistic`**.
1. Add `is_cannibalistic` to Entity `__init__`.
2. Add mutation logic in `Universe.tick()`.
3. In `get_preys_at`, if entity is cannibalistic and energy < 0.5 max, add same species entities to preys list.
4. Add visualizer render logic (e.g. `X` or `c` or `U`).
5. Add unit tests for mutation and cannibalistic combat.
6. Add to `agents.md` Next Steps (as planned) and then immediately move it to Completed.

Wait, looking at `get_preys_at` again:
```python
    def get_preys_at(self, x, y, entity=None):
        preys = [e for e in self.entities if e.x == x and e.y == y and e.diet in ['herbivore', 'scavenger', 'omnivore'] and e.is_alive and e != entity]
        if entity and getattr(entity, 'is_cannibalistic', False) and entity.energy < entity.max_energy * 0.5:
            # Cannibals can eat their own species
            same_species = [e for e in self.entities if e.x == x and e.y == y and e.species == entity.species and e.is_alive and e != entity and e not in preys]
            preys.extend(same_species)
        if entity and entity.target_species is not None:
            preys = [p for p in preys if p.species in entity.target_species]
        if entity and entity.energy >= entity.max_energy * 0.3:
            preys = [p for p in preys if not getattr(p, 'is_aposematic', False)]
        return preys
```
Also `get_preys_at` is used to FIND preys for pathfinding. But pathfinding logic loops over `self.entities` directly?
Let's check `engine.py` to see how carnivores find prey.
```python
                elif entity.diet == 'carnivore' and not getattr(entity, 'is_parasitic', False):
                    # ...
                    if entity.energy <= entity.max_energy * 0.8:
                        # Find nearest prey
                        nearest_prey = None
                        min_dist = float('inf')
                        for other in self.entities:
                            if other != entity and other.is_alive and other.diet in ['herbivore', 'scavenger', 'omnivore']:
                                # wait, if cannibalistic, it needs to find own species too
```
Ah! Pathfinding for carnivore:
```python
                            if other != entity and other.is_alive and (other.diet in ['herbivore', 'scavenger', 'omnivore'] or (getattr(entity, 'is_cannibalistic', False) and other.species == entity.species and entity.energy < entity.max_energy * 0.5)):
```
If we do this, cannibalism works.

What about something cleaner that doesn't need modifying 3 separate pathfinding spots?
How about **`has_long_legs`**?
Entities with `has_long_legs` move at half stamina cost (or get a flat speed bonus, moving twice per tick).
To move twice per tick, we'd need to modify the time % size check.
```python
            can_move = self.time % max(1, entity.size) == 0
```
If `has_long_legs`: `can_move = True` always? No, they move faster.

How about **`is_toxic_corpse`**? When they die, their meat is toxic.
When meat is spawned:
```python
            if not entity.is_alive and not getattr(entity, 'was_eaten', False):
                meat = Food(x=entity.x, y=entity.y, energy=entity.max_energy // 2, plant_type='meat', max_age=50, toxicity=entity.toxicity)
```
Wait, toxicity is already transferred to meat!

How about **`has_camouflaged_scent`** / `is_scentless`? (Already done).

How about **`has_horns`**?
When attacked by a predator, the predator takes 5 damage (energy loss) and 10 stamina loss.
Wait, `has_spikes` already does exactly this:
```python
                        if getattr(prey_to_eat, 'has_spikes', False):
                            entity.energy = max(0, entity.energy - 5)
                            entity.stamina = max(0, getattr(entity, 'stamina', 50) - 10)
```

How about **`is_acidic`**?
When eaten, it deals massive damage or permanently reduces max_energy of the predator.

How about **`can_leap`** trait?
Instead of walking around walls, they can hop over 1-tile walls.
Pathfinding change would be too complex.

Let's do **`is_pack_animal`** / `is_herd_animal`.
We have `pack_hunter`. What about herbivore herds?
Wait, we have `herd_members` logic in combat:
```python
                        herd_members = [e for e in self.entities if e.species == prey_to_eat.species and e != prey_to_eat and e.is_alive and not e.is_sleeping and abs(e.x - prey_to_eat.x) + abs(e.y - prey_to_eat.y) <= 3]
                        herd_bonus = sum(0.5 * e.defense for e in herd_members)
```

How about **`has_strong_stomach`**?
Entities with this trait are completely immune to food poisoning (toxicity) and disease from corpses.
We have `is_immune` which makes them immune to disease (`is_infected`).
`poison_resistance` reduces poison time.
`has_strong_stomach` could make them gain 2x energy from `meat` and immune to all toxicity.

Let's implement **`is_scavenger_specialist`** - wait, we have diet='scavenger'.

Let's implement **`has_prehensile_tail`**.
Entities with this trait can hold a tool in their tail, increasing effective attack by 2.
Wait, we have inventory.

Let's implement **`is_photosynthetic_carnivore`**? We have `can_photosynthesize`.

How about **`is_symbiotic_host`**?
If an entity has `is_symbiotic_host`, smaller entities on the same tile slowly heal and the host gains a tiny amount of experience.

How about **`is_arboreal`** trait?
Entities with `is_arboreal` take 0 stamina cost when moving on `forest` tiles and gain +5 defense when on `forest` tiles.
Let's check `is_forestal`:
```python
                        if getattr(prey_to_eat, 'is_forestal', False) and any(t.terrain_type == 'forest' for t in self.get_terrains_at(prey_to_eat.x, prey_to_eat.y)):
                            effective_defense += 3
```
So `is_forestal` already gives defense in forest.

How about **`is_burrower_predator`**?

How about **`has_thick_hide`**?
Reduces all damage?

How about **`can_mimic`**?
Herbivores with `can_mimic` can mimic carnivores. Predators won't attack them.
In `get_preys_at`:
```python
        preys = [e for e in self.entities if e.x == x and e.y == y and e.diet in ['herbivore', 'scavenger', 'omnivore'] and e.is_alive and e != entity]
        # If can_mimic, they are ignored by predators
        preys = [p for p in preys if not getattr(p, 'can_mimic', False) or entity.energy < entity.max_energy * 0.1]
```
Oh, like `is_aposematic`:
```python
        if entity and entity.energy >= entity.max_energy * 0.3:
            preys = [p for p in preys if not getattr(p, 'is_aposematic', False)]
```

How about **`is_hive_mind`**?

How about **`is_radioactive`**?

How about **`has_sticky_tongue`**?
Entities with this trait can catch `is_flying` insects (entities).

Let's implement **`can_regrow_limbs`**. (Similar to `is_regenerative`).

Let's implement **`is_scout`**.
Increases `perception_radius` by 5 dynamically, and shares seen food with nearby same-species entities.

Let's implement **`is_endurance_runner`**.
Trait: `is_endurance_runner`.
Entities with this trait have double the max stamina, and stamina recovery is doubled.

Let's implement **`is_agile`**.
Trait: `is_agile`.
Entities with this trait have a 50% chance to completely dodge an attack, resulting in 0 energy loss and immediate escape.
During combat:
```python
                        if getattr(prey_to_eat, 'is_agile', False) and random.random() < 0.5:
                            escape_chance = 1.0 # Guaranteed escape
```

Let's check combat escape:
```python
                        if random.random() < escape_chance:
                            # Prey escapes
```
If we add `is_agile`, we can do:
```python
                        if getattr(prey_to_eat, 'is_agile', False) and random.random() < 0.25:
                            escape_chance = 1.0
```
This is a very solid, distinct combat mechanic!
Wait, if `escape_chance = 1.0`, `random.random() < 1.0` is true.
Let's call the trait **`is_agile`**.
- Entities with `is_agile` gain a flat 25% chance to dodge and escape any attack.
- They also recover stamina slightly faster.

Let's check if there are other good ideas.
How about **`is_heavy_sleeper`**?
How about **`has_night_vision`**? (Already have `vision_type='night_vision'`).

How about **`is_vengeful`**?
If an entity is attacked and escapes, it gains +5 attack permanently.

How about **`is_kleptoparasite`**?
Can steal food from other entities.

Let's go with **`is_agile`**.
1. **Description**: Implemented `is_agile` trait. Entities with this trait have a 25% chance to completely dodge and escape any predator attack regardless of stats, and recover stamina faster.
2. **Implementation in `engine.py`**:
   - `__init__`: `is_agile=False`
   - Reproduction mutation logic for `is_agile`.
   - In `resolve_combat` (inside carnivore and omnivore logic):
     ```python
     if getattr(prey_to_eat, 'is_agile', False) and random.random() < 0.25:
         escape_chance = 1.0
     ```
   - In stamina recovery (end of tick):
     ```python
     recovery = 5 if entity.is_sleeping else 2
     if getattr(entity, 'is_agile', False):
         recovery += 3
     ```
3. **Tests**:
   - `test_is_agile_mutation`
   - `test_is_agile_dodge` (mock random to 0.1, ensure escape even with terrible stats)
   - `test_is_agile_stamina_recovery`
4. **Visualizer**:
   - Add `elif getattr(entity, 'is_agile', False): char = 'a'`
   - Wait, `a` is used? Let's check `src/universe/visualizer.py` for 'a' or 'A'.

Let's check `visualizer.py` characters:
Plan: Add is_adaptable trait