I need to add the next step. Wait, agents.md has no items in the next steps except a checked one:
- [x] Implement advanced group hunting tactics for entities with the `pack_hunter` trait (e.g. flanking, cutting off escape routes based on terrain).

What if I implement a new trait?
How about `is_scavenger_specialist`? Or `has_horns`?

What was the last thing added?
`is_volcanic`, `is_forestal`, `is_social`.

Let's add `has_horns`.
"Entities with this trait gain an attack bonus during combat." No, `has_claws` already does that.
"Entities with this trait gain a small defense bonus and an attack bonus." No.

Let's add `is_arboreal` trait. Entities with this trait can only move onto `tree` or `forest` or passable terrain but prefer trees?

Let's look at biomes. We have sand, water, mud, deep-water, ash, grass, forest, mountain.
Let's add `is_mountainous` trait. Entities with this trait gain an energy efficiency bonus when traversing mountain terrain and ignore uphill costs. No, we already have `is_desertic` and `is_forestal`. `is_mountainous` makes sense!

Wait! Let's check `src/universe/engine.py` for `is_mountainous` or `has_horns`.

```python
grep -i "is_mountainous" src/universe/engine.py
```
Let's find all terrain types.
