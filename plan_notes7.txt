The user tried to run an edit tool but failed (they sent a massive broken edit string that didn't apply). I will do it correctly using bash scripts, using python text replacement.

Here is the exact plan I will execute next:
1. `src/universe/engine.py`:
   - Add `is_vengeful=False` to `Entity.__init__` signature, and `self.is_vengeful = is_vengeful` to assignments.
   - Add mutation logic:
     `child_is_vengeful = getattr(entity, 'is_vengeful', False)`
     and `if random.random() < mutation_chance: child_is_vengeful = not child_is_vengeful`
     and append `is_vengeful=child_is_vengeful` to the `Entity()` constructor call.
   - Add combat escape logic:
     Search for `prey_to_eat.defense += 0.5`
     After that line, add:
     ```python
                            if getattr(prey_to_eat, 'is_vengeful', False):
                                prey_to_eat.attack += 1
     ```
2. `src/universe/visualizer.py`:
   - Find `char = ':'` (which is `is_restless`)
   - Add `is_vengeful` representation with `char = ';'`
3. `tests/test_engine.py`:
   - Add tests `test_is_vengeful_mutation` and `test_is_vengeful_combat`.
4. `tests/test_visualizer.py`:
   - Add `test_render_is_vengeful`.
5. `agents.md`:
   - Append to `## Next Steps`
