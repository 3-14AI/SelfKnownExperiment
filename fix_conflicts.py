# The terminal says "All conflicts fixed" but wait, did it auto-resolve or what?
# "Automatic merge failed; fix conflicts and then commit the result."
# Oh wait, `git diff --name-only --diff-filter=U` was empty?
# Let's check `git diff` for conflict markers.
